import { StubPage } from "@/components/hero/Stubpage/StubPage";

export default function ApiReferencePage() {
  return (
    <StubPage
      title="API Reference"
      description="Complete reference for ChainPay REST APIs, webhooks, and IPN — everything you need to build with crypto payments."
    />
  );
}
